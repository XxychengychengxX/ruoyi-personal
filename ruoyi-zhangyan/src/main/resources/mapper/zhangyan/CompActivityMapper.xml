<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.zhangyan.mapper.CompActivityMapper">
    
    <resultMap type="CompActivity" id="CompActivityResult">
        <result property="activityId"    column="activity_id"    />
        <result property="activityName"    column="activity_name"    />
        <result property="startTime"    column="start_time"    />
        <result property="endTime"    column="end_time"    />
        <result property="status"    column="status"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <resultMap id="CompActivityCompCompetitionResult" type="CompActivity" extends="CompActivityResult">
        <collection property="compCompetitionList" ofType="CompCompetition" column="activity_id" select="selectCompCompetitionList" />
    </resultMap>

    <resultMap type="CompCompetition" id="CompCompetitionResult">
        <result property="competitionId"    column="competition_id"    />
        <result property="activityId"    column="activity_id"    />
        <result property="competitionName"    column="competition_name"    />
        <result property="type"    column="type"    />
        <result property="startTime"    column="start_time"    />
        <result property="endTime"    column="end_time"    />
        <result property="groupType"    column="group_type"    />
        <result property="maxTeamMembers"    column="max_team_members"    />
    </resultMap>

    <sql id="selectCompActivityVo">
        select activity_id, activity_name, start_time, end_time, status, create_by, create_time, update_by, update_time from comp_activity
    </sql>

    <select id="selectCompActivityList" parameterType="CompActivity" resultMap="CompActivityResult">
        <include refid="selectCompActivityVo"/>
        <where>  
            <if test="activityName != null  and activityName != ''"> and activity_name like concat('%', #{activityName}, '%')</if>
            <if test="startTime != null "> and start_time = #{startTime}</if>
            <if test="endTime != null "> and end_time = #{endTime}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>
    
    <select id="selectCompActivityByActivityId" parameterType="Long" resultMap="CompActivityCompCompetitionResult">
        select activity_id, activity_name, start_time, end_time, status, create_by, create_time, update_by, update_time
        from comp_activity
        where activity_id = #{activityId}
    </select>

    <select id="selectCompCompetitionList" resultMap="CompCompetitionResult">
        select competition_id, activity_id, competition_name, type, start_time, end_time, group_type, max_team_members
        from comp_competition
        where activity_id = #{activity_id}
    </select>

    <insert id="insertCompActivity" parameterType="CompActivity" useGeneratedKeys="true" keyProperty="activityId">
        insert into comp_activity
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="activityName != null and activityName != ''">activity_name,</if>
            <if test="startTime != null">start_time,</if>
            <if test="endTime != null">end_time,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="activityName != null and activityName != ''">#{activityName},</if>
            <if test="startTime != null">#{startTime},</if>
            <if test="endTime != null">#{endTime},</if>
            <if test="status != null">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateCompActivity" parameterType="CompActivity">
        update comp_activity
        <trim prefix="SET" suffixOverrides=",">
            <if test="activityName != null and activityName != ''">activity_name = #{activityName},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where activity_id = #{activityId}
    </update>

    <delete id="deleteCompActivityByActivityId" parameterType="Long">
        delete from comp_activity where activity_id = #{activityId}
    </delete>

    <delete id="deleteCompActivityByActivityIds" parameterType="String">
        delete from comp_activity where activity_id in 
        <foreach item="activityId" collection="array" open="(" separator="," close=")">
            #{activityId}
        </foreach>
    </delete>
    
    <delete id="deleteCompCompetitionByActivityIds" parameterType="String">
        delete from comp_competition where activity_id in 
        <foreach item="activityId" collection="array" open="(" separator="," close=")">
            #{activityId}
        </foreach>
    </delete>

    <delete id="deleteCompCompetitionByActivityId" parameterType="Long">
        delete from comp_competition where activity_id = #{activityId}
    </delete>

    <insert id="batchCompCompetition">
        insert into comp_competition( competition_id, activity_id, competition_name, type, start_time, end_time, group_type, max_team_members) values
        <foreach item="item" index="index" collection="list" separator=",">
            ( #{item.competitionId}, #{item.activityId}, #{item.competitionName}, #{item.type}, #{item.startTime}, #{item.endTime}, #{item.groupType}, #{item.maxTeamMembers})
        </foreach>
    </insert>

</mapper>